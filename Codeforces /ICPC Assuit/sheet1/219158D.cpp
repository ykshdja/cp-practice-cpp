#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int a,b,c,d,x;
    cin>>a>>b>>c>>d;
    x = ((a*b)-(c*d));
    cout<<"Difference = "<< x<<endl;
    return 0;
}