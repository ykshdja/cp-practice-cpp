#include <bits/stdc++.h>
using namespace std;

void solve() {
    long long T;
    cin>>T;
    int D = 1 + 8*T;
    int s;
    s = sqrt(D);
    if(s*s == D)
    

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t=1;
    while (t--) {
        solve();
    }
    return 0;
}